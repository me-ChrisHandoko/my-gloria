# Database Configuration
DB_HOST=localhost
DB_PORT=3479
DB_USER=postgres
DB_PASSWORD=your-database-password
DB_NAME=gloria_v2
DB_SSLMODE=disable

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production

# Server Configuration
PORT=8080
ENV=development

# SMTP Configuration (Postmark)
# Get your Server API Token from: https://account.postmarkapp.com/servers
SMTP_HOST=smtp.postmarkapp.com
SMTP_PORT=2525
SMTP_USERNAME=your-postmark-server-token
SMTP_PASSWORD=your-postmark-server-token
SMTP_FROM=noreply@gloriaschool.org
SMTP_ENCRYPTION=tls

# Development Email (all emails in dev mode go here)
DEV_EMAIL_RECIPIENT=christian_handoko@gloriaschool.org
APP_ENV=development
